# Fusion Digest pipeline dependencies (installed at run time; PyPI is reachable
# even under the restricted egress policy).
feedparser>=6.0        # RSS/Atom + arXiv API parsing (discovery)
trafilatura>=1.6       # HTML main-text extraction (deep dive, news)
markitdown>=0.0.1      # PDF/doc -> text for primary papers (optional but recommended)
pdfminer.six>=20221105 # fallback PDF text extraction
